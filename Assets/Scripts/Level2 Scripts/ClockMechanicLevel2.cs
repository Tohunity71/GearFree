using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ClockMechanicLevel2 : MonoBehaviour
{
    private int currentTime;
    private int Oclock;


    // Update is called once per frame
    void Update()
    {
        //currentTime = Oclock.currentOclock; 
        Clock();
    }

    private void Clock()
    {
        switch (currentTime)
        {
            case 12:

                break;
        }
    }
}
